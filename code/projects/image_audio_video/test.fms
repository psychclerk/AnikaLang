win = ui_window("Media Demo", 760, 560)

# --- image: load a handle, draw on it, show it in a live view ---
h = img_load("photo.png")
h2 = img_draw_text(h, "AnikaLang", 12, 12, 28, "#ffffff")
view = ui_image_view(win, "", 10, 10, 360, 260)
ui_image_view_set_handle(view, h2)
img_close(h)
img_close(h2)

# -- audio player widget (loads + plays with full transport) ---
ap = ui_audio_player(win, "song.mp3", 10, 285, 360, 110)

# --- video: probe it, grab a thumbnail, then play it ---
# info = video_info("clip.mp4")
# video_thumbnail("clip.mp4", "thumb.png", 2.0)
#vp = ui_video_player(win, "clip.mp4", 390, 10, 360, 320)

# --- a one-shot sound effect that overlaps the music ---
#ui_button(win, "Ding", "ding", 390, 345, 100, 32)
# def ding() { sfx_play("ding.wav", 0.8) }

ui_mainloop(win)